/**
 * Placeholder value to indicate that the value was not provided. Use this for preference over undefined, as undefined is sometimes returned by functions intentionally.
 */
export const notFound: unique symbol = Symbol('notFound');
